# ── 前端环境变量 ─────────────────────────────────────────────
# Vite 自动加载此目录的 .env 文件；VITE_ 前缀的变量暴露到浏览器。
# 复制到 .env 后启动：cp .env.example .env

# Vite dev server 端口（默认 9991）
VITE_PORT=9991

# 后端端口，用于 Vite proxy 配置（默认 9990）
VITE_API_PORT=9990

# ── 生产构建 ─────────────────────────────────────────────────
# 生产模式下前端静态文件由后端直接 serve，/api 走同源，无需配置。
# 如有特殊部署需求（前后端不同源），取消注释：
# VITE_API_ORIGIN=http://127.0.0.1:9990
