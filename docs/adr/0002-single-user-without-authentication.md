# ADR 0002：第一版采用无认证的单用户系统

- 状态：已接受
- 日期：2026-08-01

## 背景

系统由单个操作者使用，没有团队协作、外部客户访问或角色分工需求。

## 决策

第一版不建设账号、登录、角色、权限控制、成员管理和多人审批能力。系统只提供一个使用空间，设置和业务数据均属于当前唯一用户。

## 影响

- 工作流和界面不显示负责人、成员或审批人。
- 数据模型不需要用户、组织和权限关联。
- 部署环境必须自行限制未授权的网络访问；如果未来开放给多人使用，需要新增身份与数据隔离设计，不能默认依赖当前边界。
